on:
  workflow_dispatch:
  schedule:
  - cron: '0 0 * * *'
name: Make Index
jobs:
  makeIndex:
    name: makeIndex
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@main
    - run: |
        bin/makeindex.ps1
    - uses: EndBug/add-and-commit@v9
      with:
        add: 'README.md'
        branch: master
        author_name: make_index-workflow
        author_email: noreply@ci.cd
        message: '(chore): update app index [scoop skip]'
